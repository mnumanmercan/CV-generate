# ─── Frontend environment variables ──────────────────────────────────────────
# Copy to .env and adjust as needed

# API base URL — points to the Express server
VITE_API_URL=http://localhost:3000/api/v1
# Production: VITE_API_URL=https://api.resumark.app/api/v1
