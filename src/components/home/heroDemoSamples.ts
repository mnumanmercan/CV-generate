/**
 * Sample names and roles cycled by the hero typewriter animation.
 * Shared between HeroMiniDemo (form placeholder) and LiveCV (preview
 * header) so both stay perfectly in sync as the same entries scroll past.
 */
export const DEMO_NAMES = ['John Doe', 'Jane Doe', 'Muhammed Ali'] as const

export const DEMO_ROLES = [
  'Senior Fullstack Developer',
  'UX&UI Designer',
  'AI Engineer',
  'Backend Architect',
] as const
